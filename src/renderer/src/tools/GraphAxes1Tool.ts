import { BaseBoxShapeTool } from '@tldraw/tldraw'

export class GraphAxes1Tool extends BaseBoxShapeTool {
	static override id = 'graph-axes-1'
	static override initial = 'idle'
	override shapeType = 'graph-axes-1'
}
